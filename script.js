window.onload = function () {
    // Masquer l'écran de préchargement une fois que la page est entièrement chargée
    var preloadScreen = document.querySelector(".preload-screen");
    preloadScreen.style.display = "none";
};